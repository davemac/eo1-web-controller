# EO1 Web Controller Configuration
# Copy this to .env and optionally customise

# Server settings (optional - defaults to port 3000 on all interfaces)
PORT=3000
HOST=0.0.0.0

# All other settings (Flickr API key, device IP, etc.) are configured
# via the web UI at http://localhost:3000 and saved to config/settings.json
